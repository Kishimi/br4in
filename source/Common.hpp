#pragma once

#include <ostream>

using i8 = char;
using i32 = int;

using u8 = unsigned char;
using u32 = unsigned int;

using s8 = signed char;
using s32 = signed int;

using Byte = u8;
using sByte = s8;